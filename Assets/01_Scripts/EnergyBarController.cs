using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class EnergyBarController : MonoBehaviour
{/*
    public Image energyBar; // Referencia al Slider en la UI
    public float maxGas = 100; // Cantidad máxima de gasolina
    public float currentGas; // Cantidad actual de gasolina

    void Start()
    {
        currentGas = maxGas; // Establece la gasolina inicial
        Update();
    }

    void Update()
    {
        energyBar.fillAmount = currentGas / maxGas; // Actualiza visualmente la barra de gas
    }

    public void AddGas(float amount)
    {
        currentGas += amount; // Agrega gasolina al jugador
        currentGas = Mathf.Clamp(currentGas, 0f, maxGas); // Asegura que no exceda el máximo
        Update();
    }

    public void ConsumeGas()
    {
        currentGas -= 10; // Consume gasolina al avanzar
        currentGas = Mathf.Clamp(currentGas, 0f, maxGas); // Asegura que no sea negativo
        Update();
    }
    */
}
